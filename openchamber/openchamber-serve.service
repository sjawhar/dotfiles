[Unit]
Description=OpenChamber fallback OpenCode backend
After=network-online.target

[Service]
WorkingDirectory=%h
ExecStart=/home/ubuntu/.mise/installs/github-sjawhar-opencode/latest/opencode serve --port 5096
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
