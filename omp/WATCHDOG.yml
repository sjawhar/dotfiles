instructions: |
  Each advisor reviews only what its own roster entry names and stays silent on everything else.

advisors:
  - name: AskGate
    enabled: true
    maxNotesPerUpdate: 1
    tools: []
    instructions: "@~/.dotfiles/omp/watchdog/askgate.md"

  - name: General
    enabled: false
    instructions: "@~/.dotfiles/omp/watchdog/general.md"
