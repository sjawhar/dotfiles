# Generated by installers/secretsd.sh — edit the installer, not this file.
# Ordered source roots: agent-tier precedence follows declaration order,
# local-over-shared within a root. The human tier has no precedence — a key
# present in two roots is refused rather than silently resolved.
[source.dotfiles]
path = "~/.dotfiles"

[source.private]
path = "~/.dotfiles/.secrets"
