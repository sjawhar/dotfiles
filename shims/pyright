#!/bin/bash
# Alias for basedpyright (Claude Code LSP compatibility)
# Use venv if available, else fall back to mise
if [[ -x .venv/bin/basedpyright ]]; then
    exec .venv/bin/basedpyright "$@"
else
    exec mise exec basedpyright -- basedpyright "$@"
fi
